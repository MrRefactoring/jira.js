import { UserSchema, type User } from './user';

/** @deprecated Renamed to `UserSchema`, which describes the same shape. This alias is removed in the next major version. */
export const DashboardUserSchema = UserSchema;

/** @deprecated Renamed to `User`, which describes the same shape. This alias is removed in the next major version. */
export type DashboardUser = User;
