import { LinkIssueRequestSchema, type LinkIssueRequest } from './linkIssueRequest';

/**
 * @deprecated Renamed to `LinkIssueRequestSchema`, which describes the same shape. This alias is removed in the next
 *   major version.
 */
export const LinkIssueRequestJsonSchema = LinkIssueRequestSchema;

/**
 * @deprecated Renamed to `LinkIssueRequest`, which describes the same shape. This alias is removed in the next major
 *   version.
 */
export type LinkIssueRequestJson = LinkIssueRequest;
