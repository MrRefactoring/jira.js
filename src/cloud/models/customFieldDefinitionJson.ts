import { CustomFieldDefinitionSchema, type CustomFieldDefinition } from './customFieldDefinition';

/**
 * @deprecated Renamed to `CustomFieldDefinitionSchema`, which describes the same shape. This alias is removed in the
 *   next major version.
 */
export const CustomFieldDefinitionJsonSchema = CustomFieldDefinitionSchema;

/**
 * @deprecated Renamed to `CustomFieldDefinition`, which describes the same shape. This alias is removed in the next
 *   major version.
 */
export type CustomFieldDefinitionJson = CustomFieldDefinition;
