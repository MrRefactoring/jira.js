import { EvaluateExpressionSchema, type EvaluateExpression } from './evaluateExpression';

/**
 * @deprecated Renamed to `EvaluateExpressionSchema`, which describes the same shape. This alias is removed in the next
 *   major version.
 */
export const EvaluateJSISJiraExpressionSchema = EvaluateExpressionSchema;

/**
 * @deprecated Renamed to `EvaluateExpression`, which describes the same shape. This alias is removed in the next major
 *   version.
 */
export type EvaluateJSISJiraExpression = EvaluateExpression;
