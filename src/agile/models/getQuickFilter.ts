import { QuickFilterSchema, type QuickFilter } from './quickFilter';

/**
 * @deprecated Renamed to `QuickFilterSchema`, which describes the same shape. This alias is removed in the next major
 *   version.
 */
export const GetQuickFilterSchema = QuickFilterSchema;

/**
 * @deprecated Renamed to `QuickFilter`, which describes the same shape. This alias is removed in the next major
 *   version.
 */
export type GetQuickFilter = QuickFilter;
