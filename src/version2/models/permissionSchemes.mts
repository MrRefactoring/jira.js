import type { PermissionScheme } from './permissionScheme.mjs';

/** List of all permission schemes. */
export interface PermissionSchemes {
  /** Permission schemes list. */
  permissionSchemes?: PermissionScheme[];
}
