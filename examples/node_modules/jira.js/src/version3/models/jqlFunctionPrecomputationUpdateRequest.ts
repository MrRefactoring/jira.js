import type { JqlFunctionPrecomputationUpdate } from './jqlFunctionPrecomputationUpdate';

/** List of pairs (id and value) for precomputation updates. */
export interface JqlFunctionPrecomputationUpdateRequest {
  values?: JqlFunctionPrecomputationUpdate[];
}
