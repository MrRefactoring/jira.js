import type * as Models from './models';
import type * as Parameters from './parameters';
import type { Client } from '../clients';
import type { Callback } from '../callback';
export declare class AppDataPolicies {
    private client;
    constructor(client: Client);
    /** Returns data policy for the workspace. */
    getPolicy<T = Models.WorkspaceDataPolicy>(callback: Callback<T>): Promise<void>;
    /** Returns data policy for the workspace. */
    getPolicy<T = Models.WorkspaceDataPolicy>(callback?: never): Promise<T>;
    /** Returns data policies for the projects specified in the request. */
    getPolicies<T = Models.ProjectDataPolicies>(parameters: Parameters.GetPolicies, callback: Callback<T>): Promise<void>;
    /** Returns data policies for the projects specified in the request. */
    getPolicies<T = Models.ProjectDataPolicies>(parameters: Parameters.GetPolicies, callback?: never): Promise<T>;
}
